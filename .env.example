# Public, non-secret placeholders only.
# Do not commit real private webhooks, tokens, API keys, service accounts, certificates, or credentials.

PUBLIC_CVG_WHATSAPP_URL="#contato"
PUBLIC_CVG_MAPS_URL="#localizacao"
