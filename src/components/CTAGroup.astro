---
import { contact } from "@/lib/site";

interface Props {
  primaryLabel?: string;
  primaryHref?: string;
  secondaryLabel?: string;
  secondaryHref?: string;
  dangerLabel?: string;
  dangerHref?: string;
  align?: "start" | "center";
  onDark?: boolean;
}

const {
  primaryLabel = "Falar com a equipe",
  primaryHref = contact.whatsappHref,
  secondaryLabel,
  secondaryHref,
  dangerLabel,
  dangerHref,
  align = "start",
  onDark = false,
} = Astro.props;
---

<div class:list={["flex flex-col gap-3 sm:flex-row", align === "center" ? "sm:justify-center" : ""]}>
  <a class="cvg-button cvg-button-primary" href={primaryHref}>{primaryLabel}</a>
  {secondaryLabel && secondaryHref && (
    <a class:list={["cvg-button", onDark ? "border-white/45 bg-white/10 text-white hover:bg-white hover:text-cvg-deep" : "cvg-button-secondary"]} href={secondaryHref}>{secondaryLabel}</a>
  )}
  {dangerLabel && dangerHref && (
    <a class="cvg-button cvg-button-danger" href={dangerHref}>{dangerLabel}</a>
  )}
</div>
